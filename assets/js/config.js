(function () {

    window.application.config = {
        base: {
            url: location.origin + '/',
        },
        api: {
            url: location.origin + '/api/v1/',
        },
    };

})();
